[build]
builder = "nixpacks"
buildCommand = "cd apps/backend && npm install --legacy-peer-deps && npm run build"

[deploy]
startCommand = "node /app/apps/backend/.medusa/server/src/main.js"
restartPolicyType = "on_failure"

[build]
builder = "nixpacks"
buildCommand = "npm install --legacy-peer-deps && cd apps/backend && npm install --legacy-peer-deps && npm run build"

[deploy]
startCommand = "cd /app/apps/backend && npm run db:migrate 2>/dev/null; npx medusa db:migrate && npm run start"
restartPolicyType = "on_failure"
